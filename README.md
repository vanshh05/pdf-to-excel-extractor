# Regulatory PDF to Excel Extractor

An offline Python tool to extract hierarchical regulatory rules (like PRA/CRR Rulebooks) into formatted Excel sheets.

## Setup Instructions

### 1. Install Python Dependencies
```bash
pip install -r requirements.txt
